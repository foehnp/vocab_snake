#ifndef game
#define game

#include "gameclass.h"
#include "funcs.h"


int play(Settings set,
       std::vector<std::string> words,
       std::vector<std::string> transs,
       sf::RenderWindow &window);
         

#endif
